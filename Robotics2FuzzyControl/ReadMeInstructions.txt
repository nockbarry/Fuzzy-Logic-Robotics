To use the fuzzy controllers you may have to open/import them into the fuzzy viewer, 
and then export them into the workspace. The fuzzy sampling time we used was .25s,
and just make sure the names that the fuzzy blocks call are the same as the ones from
the workspace